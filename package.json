{
  "name": "root",
  "private": true,
  "scripts": {
    "dev:server": "npx lerna run dev --scope=server",
    "dev:client": "npx lerna run dev --scope=client",
    "dev": "lerna run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
